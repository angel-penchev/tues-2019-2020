#include "person.hh"

// Init-ва го със стойностите, иначе първо са празни, после се копират
Person::Person(std::string first_name, std::string last_name, uint age) : first_name(first_name), last_name(last_name), age(age) {}

// std::ostream& operator<<(std::ostream& os)

// std::ostream& operator<<(std::ostream& os)